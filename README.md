# RTL_UART
Building uart RTL communication FW from scratch

**Attention** pip install ghdl -> installe un paquet "ghdl" qui n'a rien à voir avec de la simulation RTL
Il faut passer via une installation systeme : sudo apt install ghdl
